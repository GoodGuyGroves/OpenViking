[project]
name = "oaasis-openviking"
version = "0.1.0"
description = "OpenViking context database with MCP server for Oaasis workspace"
requires-python = ">=3.10"
dependencies = [
    "openviking>=0.3.9",
    "fastmcp>=3.2.0",
    "uvicorn>=0.44.0",
]
